# Phase 0 Baseline — Refactor Session 28 April 2026

## Build metrics (pre-refactor)

| Metric | Value |
|---|---|
| `App.jsx` line count | 32,355 |
| Total `src/` line count (incl. existing components) | 40,016 |
| Vite build time | ~9.2s (vite-only) / ~17.3s (npm wrapped) |
| Modules transformed | 354 |
| Bundle size (minified) | 1,927.11 kB |
| Bundle size (gzipped) | 491.61 kB |
| Bundle hash | `index-CwC2EtYG.js` |
| Sourcemap warnings | 1 pre-existing (`db.js` mixed import — cosmetic) |
| Chunk size warnings | 1 pre-existing (>500kB — known) |

## Acceptable end-of-refactor envelope (±5%)

| Metric | Min | Max |
|---|---|---|
| Bundle size (minified) | 1,830 kB | 2,024 kB |
| Bundle size (gzipped) | 467 kB | 516 kB |
| Build time (vite) | 7.4s | 11.0s |

If end-state lands outside these bounds: investigate before declaring done. Bundle bloat usually means an accidentally-kept barrel re-export or a circular dep.

## Production telemetry baseline (last 24h pre-P0)

- `pa_error_log`: **0 hits** (clean)
- `usage_events`: 7 events, 1 unique user (Connor's testing session)
  - 2× `model_route=simple` + 1× `model_route=complex` → ~67/33, matches calibrated ratio
  - 1× `tool_call=convert_quote_to_invoice`
  - 1× `tool_call=send_invoice`
  - 1× `tool_call=list_quotes`
  - 1× `voice_session=handsfree_started` + 1× `handsfree_ended`

**Implication during refactor:** any new entries in `pa_error_log` are signal — investigate before continuing. Real-user activity in `usage_events` is fine; refactor-related test activity from Connor adds noise but is identifiable by user_id.

## Snapshot branch

- Local: `refactor-snapshot-2026-04-27` → ref `9d7ee457`
- **Connor: push this branch to GitHub before P1 starts** so it survives session reset:
  ```
  git push origin refactor-snapshot-2026-04-27
  ```

## New empty directories (this commit)

```
trade-pa/src/theme/
trade-pa/src/auth/
trade-pa/src/views/
trade-pa/src/views/hubs/
trade-pa/src/modals/
trade-pa/src/settings/
trade-pa/src/notifications/
trade-pa/src/calls/
trade-pa/src/ai/
```

Each contains a single `.gitkeep`. Existing `lib/`, `hooks/`, `components/` directories are untouched — files added to them in subsequent phases.

## Pre-existing anomaly noted (not blocking)

`trade-pa/src/lib/useOnlineStatus.js` (36 lines) and `trade-pa/src/hooks/useOnlineStatus.js` (36 lines) both exist. Likely duplicate from earlier reorg. Not addressing in this refactor — flag for cleanup after.

## Phase 0 verification

- [x] Repo cloned, deps installed, build clean (exit 0, 9.15s)
- [x] Snapshot branch created (local — needs push)
- [x] 9 new directories scaffolded with `.gitkeep`
- [x] Re-built post-scaffold: identical hash + size (no drift)
- [x] Supabase telemetry baseline captured
- [x] P10 smoke checklist drafted (`/docs/2026-04-28_phase10_smoke_checklist.md`)
